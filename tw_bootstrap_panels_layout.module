<?php

/**
 * Implements hook_ctools_plugin_directory()
 */
function tw_bootstrap_panels_layout_ctools_plugin_directory($module, $plugin) {
  if ($module == "panels" && $plugin == 'layouts') {
    return 'plugins/layouts';
  }
}